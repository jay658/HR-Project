import { createTheme } from '@mui/system'

const defaultTheme = createTheme({
  palette: {
    primary: {
      main: '#1976d2',
      contrastText: 'white'
    }
  }
})

//usage: https://mui.com/system/styled/

export default defaultTheme